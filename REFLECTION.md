# Эргэцүүлэл (Reflection)

**1. Аль assertion (тест) хамгийн их үнэ цэнэтэй санагдсан бэ? Яагаад?**
Бизнес дүрмийн баталгаажуулалт (Business rule: Price is valid буюу үнэ 0-ээс их байх) болон JSON Schema validation хамгийн үнэ цэнэтэй санагдлаа. Учир нь status code 200 буцаж ирэх нь систем алдаагүй ажилласныг илтгэх ч, буцаж ирж буй өгөгдөл фронтенд талд шаардлагатай бүтцээрээ байгаа эсэх, логикийн хувьд зөв эсэхийг зөвхөн schema болон утгын шалгалт л баталж чаддаг.

**2. Negative test 1+ жишээгээ дэлгэрэнгүй тайлбарла — яг ямар алдааг олох вэ?**
Миний collection дотор "Get Non-existent Product (404)" болон "Invalid Create Product (400)" гэсэн 2 negative test байгаа. Жишээлбэл, 404 шалгалт нь мэдээллийн санд огт байхгүй `999999` гэсэн ID явуулж шалгадаг. Энэ нь систем маань байхгүй өгөгдөл хайх үед унахгүй (crash), харин зориуд 404 (Not Found) status буцааж зөв handle хийж чадаж байгаа эсэхийг баталгаажуулдаг алдааны замын (sad path) чухал шалгалт юм.

**3. Postman дотор амжилттай ажиллаж байсан тест Newman-д орвол fail болсон уу? Яагаад?**
Тийм ээ, анх Newman дээр ажиллуулахад fail болох магадлалтай тохиолдлууд гардаг. Хамгийн түгээмэл шалтгаан нь environment variable-ийг зөв зааж өгөөгүй буюу `-e postman/env.dev.json` гэж туг (flag) дамжуулаагүйгээс болж `{{baseUrl}}` нь солигдохгүй алдаа заадаг. Мөн local server маань ажиллаагүй үед эсвэл DB дотор урьдчилсан дата байхгүйгээс хамаарч fail болдог.

**4. Token эсвэл secret-ыг хэрхэн зохицуулсан бэ? (env-ийн соёл)**
Манай төсөл шууд token буцаах хэсэг нь бүрэн нэвтрээгүй ч, хэрвээ байгаа бол token-ийг хэзээ ч `collection.json` дотор hardcode хийхгүй. Үүний оронд Login request хийгдэх үед `pm.environment.set("token", json.token)` гэж авч Environment хувьсагчид хадгалаад дараагийн request-үүд `{{token}}` байдлаар уншиж ашиглана. Github руу commit хийхдээ жинхэнэ secret агуулсан environment файлыг `.gitignore`-д хийж, харин `env.ci.json` дотор хоосон placeholder үлдээдэг.

**5. API өөрчлөгдвөл collection-ийн аль хэсэг ХАМГИЙН ИХ эвдрэх вэ? Үүнийг тестийн "эмзэг газар" гэж нэрлэдэг — яаж бууруулах вэ?**
API-ийн response schema (JSON бүтэц) эсвэл endpoint-ийн URL өөрчлөгдвөл хамгийн түрүүнд эвдэрнэ. Ялангуяа нэг утгыг нөгөө рүү дамжуулж байгаа Chain хийсэн хэсэг (жишээ нь `createdProductId`) маш эмзэг. Хэрвээ POST үйлдэл ID буцаахаа боливол дагаад GET, PUT, DELETE үйлдлүүд бүгд fail болно. Үүнийг бууруулахын тулд:
- API өөрчлөлтийг хянаж урьдчилан гэрээгээ (contract) тогтоох.
- Test script дотор `if(jsonData.id)` гэх мэт хамгаалалтын нөхцөл бичиж, хэрвээ байхгүй бол тодорхой алдаа хэвлэдэг байх.
- Hardcode URL ашиглахгүйгээр бүгдийг хувьсагч (`{{baseUrl}}`) болгон салгах нь эмзэг байдлыг багасгана.
